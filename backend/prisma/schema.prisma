
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


model Vacancy {
  id    String    @id @unique @default(uuid())
  title  String
  description String?
  salary String?
}

